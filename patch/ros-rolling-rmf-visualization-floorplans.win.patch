diff --git a/CMakeLists.txt b/CMakeLists.txt
index f498d456..7427243c 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -5,6 +5,12 @@ if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
   add_compile_options(-Wall -Wextra -Wpedantic)
 endif()
 
+if(MSVC)
+  # M_PI is a non-standard extension MSVC only defines when this is
+  # set (used in src/FloorplanVisualizer.cpp).
+  add_compile_definitions(_USE_MATH_DEFINES)
+endif()
+
 include(GNUInstallDirs)
 
 find_package(eigen3_cmake_module REQUIRED)
