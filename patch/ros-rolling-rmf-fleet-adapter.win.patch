diff --git a/CMakeLists.txt b/CMakeLists.txt
index 0588eeac..ae06fc23 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -10,6 +10,13 @@ if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
 add_compile_options(-Wall -Wextra -Wpedantic)
 endif()
 
+if(MSVC)
+  # M_PI et al. are non-standard extensions MSVC only defines when this
+  # is set (used across rmf_traffic/rmf_utils's installed headers and
+  # this package's own agv/RobotContext.hpp, agv/EasyFullControl.cpp).
+  add_compile_definitions(_USE_MATH_DEFINES)
+endif()
+
 find_package(ament_cmake REQUIRED)
 find_package(backward_ros REQUIRED)
 find_package(Eigen3 REQUIRED)
