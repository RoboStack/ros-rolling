diff --git a/building_map/level.py b/building_map/level.py
index fcb46b3..3c7ce1f 100644
--- a/building_map/level.py
+++ b/building_map/level.py
@@ -430,7 +430,10 @@ class Level:
             b.append(verts[i-1] - verts[i])
         # cross products of the four pairs of vectors. If the four cross
         # products have the same sign, then the point is inside the rectangle
-        cross = np.cross(a, np.array(b))
+        # numpy>=2.0 removed support for 2D-vector cross products (used to
+        # return the scalar z-component); compute it directly instead.
+        b_arr = np.array(b)
+        cross = a[:, 0] * b_arr[:, 1] - a[:, 1] * b_arr[:, 0]
         if np.all(cross >= 0) or np.all(cross <= 0):
             return True
         else:
